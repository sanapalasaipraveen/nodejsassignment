<!DOCTYPE html>
<html>
    <head>
        <title>Express TODO App</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
        <link href="/style.css" rel="stylesheet">
    </head>
    <body>
        <header>
            Assignment 4
        </header>
        <div class="container">

            <% users.forEach((user)=>{ %>
                <div class = "container" style="border: 1px dotted;margin: 5px;padding: 5px;">
                    <div  class= "null">
                        <h3 class="<%=user.isPromoted%>"><%= user.name%></h3>
                        <p class="<%=user.isPromoted%>"> <%= user.email%></p>
                    </div>
                    <form style="display: inline-block;" method="POST" action="/users/<%= user._id %>?_method=PUT">
                        <button class="btn btn-success" id="edit-<%= user.name %>" type="submit">Promoted/Demoted</button> 
                        <!-- id="edit-<%= user.name %>" -->
                    </form>
                    <form style="display: inline-block;" method="POST" action="/users/<%= user._id %>?_method=DELETE">
                        <button class="btn btn-danger" id="edit-<%= user.name %>">Delete</button>
                    </form>
                </div>
            <%}) %>
        </div>
        <a href="/form">Add User</a>
    </body>
</html> 